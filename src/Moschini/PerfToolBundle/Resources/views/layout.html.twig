{% extends "::base.html.twig" %}

{% block title '' %}

{% block body %}
        <ul class="nav nav-tabs" id="menu-nav">
            <li><a href="{{ path('moschini_perftool_default_time') }}">Monitoring</a></li>
            <li><a href="{{ path('moschini_perftool_default_graph') }}">Histograms</a></li>
            <li><a href="{{ path('moschini_perftool_default_index') }}">Waterfall</a></li>
            <li class="pull-right"><a href="{{ path('moschini_perftool_sites_index') }}">Manage sites</a></li>
        </ul>

        {% for flashMessage in app.session.flashbag.get('notice') %}
            <div class="flash-message">
                <em>Notice</em>: {{ flashMessage }}
            </div>
        {% endfor %}

        <div class="block">
            {% block content %}{% endblock %}
        </div>

{% endblock %}
